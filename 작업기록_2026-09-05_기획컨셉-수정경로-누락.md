# 작업기록 — 2026-09-05 기획 컨셉이 수정·재생성 경로에서 빠지던 것

## 배경
자야쌤이 "작업자가 만든 캐러셀이 한 캐러셀 안에서 3~5가지 이야기를 서두로 계속 꺼낸다"고 느껴
2026-09-04에 '기획 컨셉' 칸(4종을 관통하는 방향)을 넣었다. 그런데 컨셉은 **첫 생성과 '전체 기획 다시하기'에만**
프롬프트에 들어가고, 그 뒤에 도는 모델 호출에는 전혀 실리지 않았다.

## 무엇이 새고 있었나 (코드로 확인)
컨셉은 어디에도 저장되지 않고 요청 메모리에만 있었다(`generateAll`/`regenerateAll`의 인자). 그래서:
- **캐러셀 수정(revise)** — `buildCarouselRevisePrompt`가 원본 프롬프트를 다시 만들 때 `buildCarouselPrompt({ voice, topic })`로
  호출해 컨셉이 빠졌다. **후크를 B안으로 바꾸는 순간 컨셉이 없어지는** 대표 사례.
- 카페·캡션 수정(`buildRevisePrompt`) — 애초에 `conceptBlock`이 없었다.
- 캐러셀 컷 수 조정 재요청(`finalizeCarousel`) — 수정 프롬프트를 쓰므로 같이 빠짐.
- 카페 수정 뒤 캡쳐 가이드 자동 갱신 — `buildCapturePrompt({ cafe })`만.
- 캐러셀·캡쳐 재생성(`regenerateSecondary` → `buildRegeneratePrompt`) — 컨셉 인자 없음.

## 어떻게 고쳤나
1. **저장**: `jobs` 테이블에 칸이 없고(Supabase 마이그레이션이 필요해짐) 파일 폴백도 있어서,
   **`contents`의 `'concept'` 타입으로 버전 저장**한다(`generate.js`의 `saveConcept`/`loadConcept`, 상수 `CONCEPT_TYPE`).
   첫 생성·전체 다시하기마다 새 버전이 쌓이므로 "언제 어떤 컨셉으로 기획했는지"가 이력에 남는다.
   파일 폴백에서는 `outputs/{jobId}/concept.md`로도 떨어진다. 스키마 변경 없음.
2. **실기**: 수정(`revise`)·재생성(`regenerateSecondary`)·컷 수 조정(`finalizeCarousel`)·캡쳐 갱신·캡션 결 맞추기가
   전부 `loadConcept(jobId)`로 최신 컨셉을 읽어 프롬프트에 넣는다.
   `prompts.js`의 `buildRevisePrompt`(세 타입 모두 `conceptBlock`)·`buildCarouselRevisePrompt`·`buildRegeneratePrompt`가 `concept`을 받는다.
3. **화면**: 지난 작업을 열면 '전체 기획 다시하기' 칸에 **저장된 컨셉이 미리 채워진다**(`app.js` `latestConceptOf`).
   전에는 빈칸이라 무엇을 기준으로 기획됐는지 알 수 없었다.
4. **테스트**: `npm test` (`test/concept.test.js`). 모델·비메오·보이스를 가짜로 바꿔 API 호출 없이
   첫 생성 → 캐러셀/카페/캡션 수정 → 재생성 → 전체 다시하기 → 다시 수정 순서로 돌리며 **매 호출의 프롬프트에 컨셉이 있는지**,
   전체 다시하기 뒤에는 **옛 컨셉이 아니라 새 컨셉**을 무는지 본다. 저장은 실제 파일 폴백을 쓰고 끝나면 지운다.
   고치기 전 코드로 돌리면 실제로 실패하는 것을 확인했다(아래).

## 주의
- 이 프로젝트에 테스트가 처음 생겼다. `package.json`에 `"test": "node --test --experimental-test-module-mocks test/"`.
  Node 22.3 이상 필요(모듈 가짜 바꾸기). 로컬은 Node 24.
- 테스트는 파일 폴백으로만 돈다 — `.env`에 `SUPABASE_URL`이 있으면 첫 단언에서 멈춘다.
- **Supabase를 쓰는 배포에서는 `contents.type`에 `'concept'` 값이 새로 들어간다.** PRD의 스키마상 `type text`에 제약이 없어 그대로 들어가지만,
  혹시 CHECK 제약을 붙여 두었다면 풀어야 한다.
- 컨셉 자체의 한계는 그대로다: 11역할 템플릿(solution 커리큘럼 목록·audience 대상 목록·detail 확대)이 컨셉 밖 소재를 다시 불러들인다.
  이건 별도 결정 사항(템플릿 계약을 '한 논지의 전진'으로 바꿀지)이고 이번 작업 범위가 아니다.

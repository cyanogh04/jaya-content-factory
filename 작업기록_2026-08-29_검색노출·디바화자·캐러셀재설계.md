# 작업기록 — 2026-08-29 검색 노출(SEO/AEO/GEO) · 카페 화자 디바 전환 · 캐러셀 기획 재설계

다음 세션이 이 기록만 읽고 바로 이어갈 수 있도록 **무엇을 / 왜 / 어떻게 / 검증은 어떻게** 순으로 적는다.
변경은 전부 `src/prompts.js`(프롬프트)와 `src/generate.js`(캡쳐 가이드 연동) 두 파일에 있다. 프론트·DB·API는 손대지 않았다.

커밋 범위: `3a2c072` → `0fd9ff2` (5건). 배포는 push → Render 자동 배포.

---

## 0. 요청의 배경 (사용자 의도)

1. 결과물이 네이버/구글/AI 검색(퍼플렉시티·제미나이·GPT)에서 검색되게 하고 싶다. **단, AI가 쓴 티가 나면 안 되고 자야쌤 말투여야 한다** — 이게 가장 큰 전제.
2. 카페 서머리가 인스타 글 느낌이라 카페와 안 맞는다. 실제로 카페에 올리는 사람은 자야쌤이 아니라 **매니저 닉네임 '디바'**다. 화자를 바꾸되 말투 온도는 자야쌤과 비슷하게. 참고용 실제 카페 글 PDF 5건이 `카페글참고/`에 있다.
3. 인스타 캡션: 수강생 닉네임은 첫 글자만 남기고 `**`로 가린다(`자**님`). **해시태그는 이제 안 쓴다** — 검색어를 본문에 스며들게.
4. (후속) 캐러셀 기획도 인스타 마케팅 스킬로 더 촘촘하게.
5. (후속) 카페 마지막 CTA는 `[소잉클래스 수강 신청하기]http://www.sewingclass.co.kr`로 끝나야 하고, 카페 본문에 캡쳐 이미지가 들어갈 자리를 표시해야 한다(디바는 강의 화면을 못 고르니까).
6. (후속) "옷을 짓는다"는 철학 문구지 실제 말버릇이 아니다. 권유문은 "함께 옷을 만들어봐요~".
7. 캡션에 후크 후보 3개 제시는 **하지 않기로 결정** — 작업자가 골라낼 능력이 없어서. 캐러셀 개요에만 후보를 둔다.

---

## 1. 검색 노출 규칙 `SEARCH_RULE` (카페·캡션·캐러셀·수정 공용)

### 왜 이렇게 했나
`claude-seo` 스킬은 URL을 감사하는 도구라 프롬프트 생성에는 직접 못 쓴다. 대신 그 안의 `seo-geo`·`seo-content` 기준(AI 인용 가능성 = 자기완결 답변 문단, 질문형 소제목, 단정 서술, 엔티티 명시, 구체 명사, 앞쪽 30%에 핵심)을 **글쓰기 규칙으로 번역**했다.
"AI티 금지"는 `humanize-korean` 플러그인의 `ai-tell-taxonomy.md`(D. 시그니처 관용구, C. 구조 공식, E. 리듬 균일성 등)에서 추려 넣었다. 검색 장치의 절반이 금지 목록인 이유: 검색에 잡혀도 AI 글로 읽히면 실패라는 전제 때문.

### 내용 요약
- 검색어 2~4개를 **문제형 / 이름형 / 대상형**으로 섞어 정하고 제목·첫 두 줄·앞쪽·마지막에 활용형을 바꿔가며 2~4회. 자막 용어 우선, 정식 용어와 구어가 다르면 둘 다(립/시보리).
- 브랜드 엔티티 `자야쌤` `소잉클래스` `디자인드바이(디바)` 글마다 1회.
- AI 인용용 **자문자답 문단** 1~2개 — 자야쌤 카페글에 원래 있는 습관("시보리는 80%로 계산하면 되는 거 아니에요? 맞는 말이기도 하고 틀린 말이기도 합니다")이라 자연스럽게 붙는다. 답은 `DISCLOSURE_RULE`의 진단 범위 안에서만.
- AI티 금지 목록 + 자가 검증("검색어·브랜드명을 다 지워도 원래 그대로 자연스러운가").

---

## 2. 카페 서머리 — 화자 디바, 결은 실제 카페 글 (`CAFE_STYLE`)

### PDF 분석 방법
`pdftotext`는 한글이 깨진다(폰트 인코딩). **`pip install pymupdf` 후 `pymupdf.open(p)[i].get_text()`** 로 추출해야 한다. 다음에 PDF 참고자료가 늘면 같은 방법.

### 분석에서 뽑은 실제 글의 특징 (프롬프트에 "실제 예)"로 인용해 둠)
- 문장마다 줄바꿈, 문단 2~3줄, 이모지 3~5개 이내(캡션보다 훨씬 성글다)
- 자문자답 소제목 줄 2~3개 ("소잉클래스 수강생만 들을 수 있나요?" / "나중에 다시 듣기 가능한가요?")
- 독자 속마음 따옴표 인용 ("자야쌤!~ 이 옷은 어떤 원단 사와요? 그냥 딱! 정해주시면 안 되요?")
- 퀴즈 후 뒤집기 ("미싱? 재단? 패턴? 🙅‍♀️ 아니요~~")
- 통념 인정 후 뒤집기, 수업 태도 대비("여기 심지 붙이세요! 이런 따라하기식 말은 자야쌤 수업에선 찾기 힘듭니다")
- 수강 안내로 마무리
- 참고: PDF의 글쓴이 표시는 'JAYa 자야'지만 사용자 설명대로 실제 게시 담당은 '디바'. 카페 이름은 '디바카페'(디자인드바이)로 댓글에 등장.

### 규칙
- 화자 **디바**(디자인드바이 카페 매니저). 자야쌤은 3인칭. "편집하면서 저도 처음 알았어요" 같은 목격자 시점 허용(자막에 없는 장면은 금지).
- 첫 줄 = 게시글 제목(검색어 포함). 앞머리 태그는 반드시 여닫는 괄호 `[강의 오픈]` — 1차 생성에서 `스판청지 원단 늘어남]`처럼 반쪽만 나와서 추가한 규칙.
- **처방이 새는 길목**: 자막 속 자야쌤 지시문을 "자야쌤이 ~하라고 하셨어요"로 옮기면 화자만 바뀐 처방 유출. 지시의 존재와 이유까지만. (1차 생성에서 "식서 방향까지 바꿔야 한다고 하세요"가 나와서 추가)
- 분량 **1,800~2,200자(공백 포함)** — 줄바꿈이 많은 결이라 2,000자 내외로는 1,450자가 나옴. 하한을 명시.
- 닉네임: **카페에선 가리지 않는다**(사용자 확인). 캡션·캐러셀만 `자**님`.
- 마지막: 이 글의 고민에 맞춘 권유 두세 줄 → 마지막 줄 `[소잉클래스 수강 신청하기]http://www.sewingclass.co.kr` 그대로. 링크 뒤엔 아무것도 안 씀.
- **캡쳐 자리표시**: 본문 3~5곳, 장면을 말하는 문단 바로 아래 `📷 캡쳐 n [MM:SS] 화면에 보이는 것 한 줄`. 타임스탬프는 압축 자막(30초 샘플)에 실제로 찍힌 시각만.
- 네이버 에디터는 마크다운 미지원 → `#`, `**`, `-` 금지.

---

## 3. 캡쳐 가이드 ↔ 카페 자리 연동

- `buildCapturePrompt({ cafe })`: 카페 글을 받으면 📷 줄 번호별 대응 항목을 **같은 번호로 먼저** 뽑고, 원본 자막으로 시각을 보정하되 카페 시각과 30초 이내. 사용처를 `카페 캡쳐 n` / `카드뉴스`로 표기.
- `generate.js` 초기 생성 루프: capture 차례에 `results.cafe`를 넘겨 프롬프트를 그때 조립(생성 순서 cafe→caption→carousel→capture라 가능). 재생성 경로(`buildRegeneratePrompt`)도 cafe 전달.
- 검증: 카페 `[15:02]` → 가이드 `[15:06] … / 카페 캡쳐 1`처럼 4개 자리 전부 대응 확인.

---

## 4. 인스타 캡션

- 해시태그 전면 금지. 대신 첫 두 줄과 CTA 문단에 검색어 하나씩. 분량 표기도 "해시태그 제외"에서 "본문 1,000~1,300자"로.
- 닉네임 마스킹 `자**님` — 예시 캡션(voice_profile)에 온전한 이름이 있어도 따라 쓰지 않도록 명시.
- 수정(revise) 프롬프트: 수정 지시에서 해시태그를 요구해도 본문 검색어로 대신하도록.

---

## 5. 캐러셀 기획 재설계 (`buildCarouselPrompt`)

### 근거 스킬
`social/references/carousel-frameworks.md`(Value-Stack·Problem-Proof·Hack List·Rant·Demo Walkthrough), `copywriting`(대안 제시·고객 언어), `marketing-psychology`(역할별 심리 장치). CLAUDE.md 절대 규칙 4(역할 순서, audience/solution 표기 규약, 첫 컷 강의 화면 금지)는 그대로.

### 추가한 [기획 원리]
- 1컷 = 썸네일(단독 노출 테스트), 후크는 자막 속 수강생 실제 말
- 열린 고리: 1컷의 질문을 **proof/result 컷에서만** 닫는다(empathy에서 원인 밝히는 건 조이는 것, 닫는 게 아님 — 1차 생성이 3컷에서 닫아버려 추가)
- 컷당 한 생각, 본문 3줄·60자
- 이름 붙인 기술(정식 용어), 지도 먼저(detail 헤드는 solution 항목명 재사용)
- 저장 유도 컷 1개 지정(저장·완주가 캐러셀 성적)
- 역할별 심리 장치: empathy=오답 인정 후 뒤집기 / audience=동류 의식 / turn=권위 근거 / proof=대비 / objection=후회 회피 / urgency=손실 회피(가짜 희소성 금지) / cta=행동 하나
- 개요 출력에 **후크 후보 3개 + 채택안**, 열린 고리 닫는 컷, 저장 유도 컷 항목 추가
- 캐러셀은 `voiceBlock`을 안 쓰므로 '짓다' 금지·대시 금지·목록 컷 디자인 지시 금지를 따로 넣음(1차 생성에서 "제대로 짓기/지어요", "번호 목록으로 배치"가 나왔음)
- 출력 전 최종 점검 9항목

---

## 6. 말투 공통 — '짓다' 금지 (`voiceBlock`)

"옷을 짓는다"는 브랜드 철학 문구(CLAUDE.md)라 모델이 권유문까지 "지어보세요"로 써버린다. 실제 말버릇은 "만들어요". 철학 문구 그대로 인용 1회만 예외, 나머지 활용형 전부 금지. 카페·캡션·수정 공용 + 캐러셀 별도.

---

## 7. 검증 방법 (다음에 프롬프트 바꾸면 이대로 재검증)

웹앱 히스토리에 안 남기고 최신 job의 자막으로 직접 호출하는 스크립트를 썼다(프로젝트 루트에 `_t.mjs` 임시 생성 후 삭제):

```js
import 'dotenv/config';
import { listJobs, getJob } from './src/db.js'; import { load as loadVoice } from './src/voice.js';
import { callClaude, MODELS } from './src/anthropic.js';
import { COMMON_HEADER, buildCafePrompt, buildCaptionPrompt, buildCarouselPrompt, buildCapturePrompt } from './src/prompts.js';
const job = await getJob((await listJobs(1))[0].id); const voice = await loadVoice();
const cafe = await callClaude({ model: MODELS.CAFE, system: COMMON_HEADER, prompt: buildCafePrompt({ voice }), transcript: job.transcript_compressed, step: 'cafe' });
const cap  = await callClaude({ model: MODELS.CAPTURE, system: COMMON_HEADER, prompt: buildCapturePrompt({ cafe }), transcript: job.transcript_full, step: 'capture' });
// 캐러셀은 maxTokens: 16000
```
- Windows ESM은 `G:/...` 절대 경로 import가 안 된다 → 상대경로 `./src/...`로.
- 테스트 대상 job: `62d6fadb…` "1강-크롭데님쟈켓". Supabase에 자막이 저장돼 있어 재사용 가능.
- 결과 파일은 `테스트결과/`(gitignore)에 있음: 카페 1차/최종, 캡쳐자리 4곳 버전 + 연동 캡쳐가이드, 캡션, 캐러셀.

검수 체크(매번 눈으로 본 것): 화자·제목 괄호·분량·처방 유출 문장·'짓/지어' 어절·대시·해시태그·닉네임·캡쳐 번호 대응·마지막 줄 링크.

---

## 8. 저장소·배포 주의

- `카페글참고/`(회원 닉네임·댓글 포함), `테스트결과/`는 **.gitignore** — 저장소가 PUBLIC.
- Render 자동 배포. 확인: `curl https://jaya-content-factory.onrender.com/api/status`의 `commit` 필드(콜드스타트 15초+).
- 프롬프트 캐시: `COMMON_HEADER`(system)는 손대지 않았다. 프롬프트 본문이 바뀌면 자막 캐시 블록은 그대로 재사용된다.

## 9. 남은 선택지 (결정 안 됨)
- 캡션 후크 후보 제시 → **안 하기로 결정**(작업자가 못 고름).
- 카페 CTA 앞 권유 문장이 가끔 "피드백도 주시니까" 같은 자막 밖 혜택을 말할 수 있음 — 눈에 띄면 CAFE_STYLE에 "혜택은 자막에 있는 것만" 한 줄 추가.
- 분기별 50만뷰+ 레퍼런스 분석으로 캐러셀 [기획 원리] 갱신(CLAUDE.md 메모와 동일 운용).
